export enum allow_comment_enum{
    allow="allow",
    deny="deny"
}

export enum availibility_enum{
    public="public",
    only_me="only_me",
    friends="friends"
}

export enum On_Model_enum{
    Post="Post",
    Comment="Comment"
}